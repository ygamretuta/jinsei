<%= semantic_form_for [@product.business, @product] do |f| %>
    <%= f.inputs do  %>
        <%= f.input :name %>
        <%= f.input :category, :collection => Category.all %>
        <%= f.input :catalog, :collection => @product.business.catalogs%>
        <%= f.buttons %>
    <% end %>
<% end %>